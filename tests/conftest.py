import pytest
from playwright.sync_api import sync_playwright

# pytest-playwright provides automatic fixtures: page, browser, browser_name, context
# No manual fixtures needed - they're auto-provided by the plugin
